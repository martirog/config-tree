# TODO

- [x] Create top level function to start parsing the tree
- [x] Add help text from docstrings in commands
- [x] Pass the path to the commands directory from read_config through execute_config, and support a list of command directories
- [x] Make sure this project can be installed with pip
- [ ] Make all needed functions accessible from one import namespace
