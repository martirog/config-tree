[metadata]
name = config-tree
version = 0.1.0
description = A Python config tree executor
requires_python = >=3.6

[options]
package_dir =
    = src
packages = find:

[options.packages.find]
where = src
